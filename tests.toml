#:schema https://raw.githubusercontent.com/YunoHost/apps/master/schemas/tests.v1.schema.json

test_format = 1.0

[default]

        # ------------
    # Tests to run
    # ------------

    preinstall = """
        sudo yunohost tools update apps
        sudo yunohost app install https://github.com/YunoHost-Apps/calibreweb_ynh/ \
        -a "domain=domain.tld&init_main_permission=all_users&upload=true&public_library=true" --force 
    """

    # -------------------------------
    # Default args to use for install
    # -------------------------------

    args.database_calibre = "/home/yunohost.multimedia/share/eBook/metadata.db"
    args.database_calibreweb = "/var/www/calibreweb/app.db"
    args.token_anilist = "123456789bépoè"
    args.token_hardcover = "123456789bépoè"
